<% provide(:title, t(:edit_competitor) ) %>

<div class="page-header">
	<h1><%= t(:edit_competitor) %></h1>
</div>

<%= render 'form' %>

<%= link_to t(:show), @competitor, class: "btn" %>
<%= link_to t(:back), competitors_path, class: "btn" %>
