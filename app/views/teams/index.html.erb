<h1>Listing teams</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Club</th>
    <th>Event</th>
    <th>Subevent</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @teams.each do |team| %>
  <tr>
    <td><%= team.name %></td>
    <td><%= team.club_id %></td>
    <td><%= team.event_id %></td>
    <td><%= team.subevent_id %></td>
    <td><%= link_to 'Show', team %></td>
    <td><%= link_to 'Edit', edit_team_path(team) %></td>
    <td><%= link_to 'Destroy', team, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Team', new_team_path %>
